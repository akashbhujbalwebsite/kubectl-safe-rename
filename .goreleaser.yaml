project_name: kubectl-safe-rename

before:
  hooks:
    - go mod tidy

builds:
  - id: kubectl-safe-rename
    main: .
    binary: kubectl-safe-rename
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - id: kubectl-safe-rename
    name_template: "kubectl-safe-rename_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        format: zip
    files:
      - LICENSE
      - README.md

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

release:
  github:
    owner: akashbhujbalwebsite
    name: kubectl-safe-rename
  name_template: "v{{.Version}}"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
